<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Gerador de Declarações - Convênio - Ministério do Esporte</title>

    <!-- Fontes oficiais GOV.BR -->
    <link rel="stylesheet" href="https://cdngovbr-ds.estaleiro.serpro.gov.br/design-system/fonts/rawline/css/rawline.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway:300,400,500,600,700,800,900&display=swap">

    <!-- Design System GOV.BR -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@govbr-ds/core@3.3.2/dist/core.min.css">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">

    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Rawline', 'Raleway', sans-serif;
            background: #EDF2FF;
            color: #333;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            padding-bottom: 60px;
        }

        /* ── BARRA DO GOVERNO ── */
        #gov-bar {
            background: #071D41;
            padding: 5px 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 8px;
        }

        .gov-bar-brand {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 13px;
            font-weight: 700;
            color: #fff;
            letter-spacing: 0.5px;
        }

        .gov-bar-links {
            display: flex;
            gap: 16px;
            list-style: none;
            flex-wrap: wrap;
        }

        .gov-bar-links a {
            font-size: 11px;
            color: rgba(255,255,255,0.65);
            text-decoration: none;
            white-space: nowrap;
        }

        .gov-bar-links a:hover { color: #fff; text-decoration: underline; }

        /* ── HEADER ── */
        .ministry-header {
            background: #fff;
            border-bottom: 1px solid #D9D9D9;
            padding: 0 24px;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 6px rgba(0,0,0,0.08);
        }

        .header-top-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 64px;
            gap: 16px;
        }

        .header-logo-area {
            display: flex;
            align-items: center;
            gap: 14px;
        }

        .govbr-badge {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 14px;
            font-weight: 700;
            color: #1351B4;
            text-decoration: none;
        }

        .divider-v {
            width: 1px;
            height: 32px;
            background: #D9D9D9;
        }

        .ministry-name {
            font-size: 15px;
            font-weight: 700;
            color: #071D41;
            line-height: 1.2;
        }

        .ministry-name small {
            display: block;
            font-size: 11px;
            font-weight: 400;
            color: #555;
        }

        .header-actions { display: flex; align-items: center; gap: 8px; }

        .icon-btn {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border: 1px solid #D9D9D9;
            background: transparent;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: #333;
            font-size: 14px;
            transition: background 0.15s;
        }

        .icon-btn:hover { background: #f0f0f0; }

        .header-bottom-row {
            padding: 7px 0;
            border-top: 1px solid #EFEFEF;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 12px;
            color: #555;
            font-weight: 500;
        }

        /* ── BREADCRUMB ── */
        .breadcrumb-bar {
            background: #F8F8F8;
            border-bottom: 1px solid #E5E5E5;
            padding: 8px 24px;
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 12px;
            flex-wrap: wrap;
        }

        .breadcrumb-bar a { color: #1351B4; text-decoration: none; }
        .breadcrumb-bar a:hover { text-decoration: underline; }
        .breadcrumb-bar .sep { color: #999; }
        .breadcrumb-bar .current { color: #333; font-weight: 600; }

        /* ── MAIN ── */
        #main-content {
            flex: 1;
            padding: 32px 24px;
            max-width: 960px;
            width: 100%;
            margin: 0 auto;
        }

        /* ── PAGE HEADER ── */
        .page-header {
            background: #fff;
            border-radius: 8px;
            border-top: 4px solid #1351B4;
            padding: 24px 28px;
            margin-bottom: 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 2px 8px rgba(19,81,180,0.07);
        }

        .page-header-left { display: flex; align-items: center; gap: 16px; }

        .page-header-icon {
            width: 48px;
            height: 48px;
            background: #EDF2FF;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #1351B4;
            font-size: 20px;
            flex-shrink: 0;
        }

        .page-header h2 {
            font-size: 18px;
            font-weight: 700;
            color: #071D41;
            font-family: 'Raleway', sans-serif;
        }

        .page-header p {
            font-size: 12px;
            color: #666;
            margin-top: 2px;
        }

        .status-badge {
            font-size: 11px;
            font-weight: 700;
            color: #1351B4;
            background: #EDF2FF;
            padding: 5px 14px;
            border-radius: 100px;
            border: 1px solid #C5D4FF;
            white-space: nowrap;
        }

        /* ── TAB STEPS ── */
        .steps-nav {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0;
            margin-bottom: 24px;
            background: #fff;
            border-radius: 8px;
            padding: 20px 24px;
            box-shadow: 0 2px 8px rgba(19,81,180,0.07);
            border: 1px solid #E5E5E5;
            overflow-x: auto;
        }

        .step-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 6px;
            position: relative;
            flex: 1;
            min-width: 90px;
        }

        .step-item:not(:last-child)::after {
            content: '';
            position: absolute;
            top: 18px;
            left: calc(50% + 20px);
            right: calc(-50% + 20px);
            height: 2px;
            background: #D9D9D9;
            z-index: 0;
        }

        .step-item.done:not(:last-child)::after { background: #1351B4; }

        .step-btn {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border: 2px solid #D9D9D9;
            background: #fff;
            color: #999;
            font-size: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            position: relative;
            z-index: 1;
            transition: all 0.2s;
        }

        .step-btn:disabled { cursor: not-allowed; }

        .step-item.active .step-btn {
            border-color: #1351B4;
            background: #1351B4;
            color: #fff;
        }

        .step-item.done .step-btn {
            border-color: #1351B4;
            background: #EDF2FF;
            color: #1351B4;
        }

        .step-label {
            font-size: 11px;
            color: #999;
            font-weight: 600;
            text-align: center;
            white-space: nowrap;
        }

        .step-item.active .step-label { color: #1351B4; }
        .step-item.done .step-label { color: #1351B4; }

        /* ── FORM CARD ── */
        .form-card {
            background: #fff;
            border-radius: 8px;
            padding: 28px;
            box-shadow: 0 2px 8px rgba(19,81,180,0.07);
            border: 1px solid #E5E5E5;
            margin-bottom: 20px;
        }

        .section-title {
            font-size: 14px;
            font-weight: 700;
            color: #1351B4;
            border-bottom: 2px solid #1351B4;
            padding-bottom: 8px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .form-row {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
            margin-bottom: 16px;
        }

        .half-width { flex: 1; min-width: 200px; }
        .third-width { flex: 1; min-width: 140px; }
        .full-width { flex: 1; min-width: 100%; }

        .field-label {
            font-size: 11px;
            font-weight: 700;
            color: #444;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 5px;
            display: block;
        }

        .govbr-input {
            width: 100%;
            padding: 10px 13px;
            border: 1px solid #888;
            border-radius: 4px;
            font-size: 14px;
            font-family: 'Rawline', 'Raleway', sans-serif;
            color: #333;
            background: #fff;
            transition: border-color 0.15s, box-shadow 0.15s;
        }

        .govbr-input:focus {
            outline: none;
            border-color: #1351B4;
            box-shadow: 0 0 0 3px rgba(19,81,180,0.18);
        }

        .govbr-input[readonly] {
            background: #F5F5F5;
            color: #777;
            cursor: not-allowed;
            border-color: #CCC;
        }

        .govbr-input.error { border-color: #C92300; }

        .error-message {
            display: none;
            font-size: 11px;
            color: #C92300;
            margin-top: 4px;
        }

        .error-message.visible { display: block; }

        /* ── OPÇÕES DE GERAÇÃO ── */
        .option-group {
            border: 1px solid #D9D9D9;
            border-radius: 8px;
            margin-bottom: 16px;
            overflow: hidden;
            transition: border-color 0.2s;
        }

        .option-group.selected { border-color: #1351B4; }

        .option-group-header {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 16px 20px;
            cursor: pointer;
            background: #FAFAFA;
            font-size: 14px;
            font-weight: 700;
            color: #333;
            transition: background 0.15s;
        }

        .option-group-header:hover { background: #EDF2FF; }

        .option-group.selected .option-group-header {
            background: #EDF2FF;
            color: #1351B4;
        }

        .option-group-header input[type="radio"] {
            accent-color: #1351B4;
            width: 18px;
            height: 18px;
            cursor: pointer;
        }

        .sub-options {
            display: none;
            padding: 20px;
            border-top: 1px solid #E5E5E5;
            background: #fff;
        }

        .sub-options.open { display: block; }

        .sub-options p {
            font-size: 12px;
            font-weight: 700;
            color: #444;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 12px;
        }

        .radio-container {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 16px;
            border: 1px solid #D9D9D9;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.15s;
            font-size: 13px;
        }

        .radio-option:hover { border-color: #1351B4; background: #EDF2FF; }

        .radio-option input[type="radio"] {
            accent-color: #1351B4;
            width: 16px;
            height: 16px;
            cursor: pointer;
        }

        .radio-option.checked { border-color: #1351B4; background: #EDF2FF; color: #1351B4; font-weight: 600; }

        /* ── CHECKBOX ── */
        .checkbox-wrapper {
            display: flex;
            align-items: center;
            gap: 10px;
            background: #F8F8F8;
            padding: 12px 16px;
            border-radius: 6px;
            border: 1px solid #E5E5E5;
            cursor: pointer;
        }

        .checkbox-wrapper input[type="checkbox"] {
            appearance: none;
            width: 18px;
            height: 18px;
            min-width: 18px;
            border: 2px solid #888;
            border-radius: 3px;
            cursor: pointer;
            position: relative;
            background: #fff;
        }

        .checkbox-wrapper input[type="checkbox"]:checked {
            background: #1351B4;
            border-color: #1351B4;
        }

        .checkbox-wrapper input[type="checkbox"]:checked::before {
            content: '✔';
            font-size: 11px;
            color: #fff;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .checkbox-wrapper label {
            font-size: 13px;
            color: #333;
            cursor: pointer;
            font-weight: 500;
        }

        /* ── ESPAÇO FÍSICO ── */
        .espaco-fisico-container {
            margin-top: 20px;
            background: #F8F8F8;
            border-radius: 8px;
            padding: 20px;
            border: 1px dashed #C5D4FF;
        }

        .espaco-fisico-container h4 {
            font-size: 13px;
            font-weight: 700;
            color: #1351B4;
            margin-bottom: 16px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .remove-btn {
            background: #C92300;
            color: #fff;
            border: none;
            padding: 8px 14px;
            border-radius: 4px;
            font-size: 12px;
            cursor: pointer;
            align-self: center;
            white-space: nowrap;
            transition: opacity 0.15s;
        }

        .remove-btn:hover { opacity: 0.85; }

        /* ── BOTÕES ── */
        .btn-row {
            display: flex;
            justify-content: flex-end;
            gap: 12px;
            margin-top: 24px;
            padding-top: 20px;
            border-top: 1px solid #EFEFEF;
        }

        .btn-secondary {
            background: transparent;
            color: #1351B4;
            border: 1.5px solid #1351B4;
            padding: 11px 24px;
            border-radius: 100px;
            font-size: 14px;
            font-family: 'Raleway', sans-serif;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.15s;
        }

        .btn-secondary:hover { background: #EDF2FF; }

        .btn-primary {
            background: #1351B4;
            color: #fff;
            border: none;
            padding: 11px 24px;
            border-radius: 100px;
            font-size: 14px;
            font-family: 'Raleway', sans-serif;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: background 0.15s, transform 0.1s;
        }

        .btn-primary:hover { background: #0C3584; }
        .btn-primary:active { transform: scale(0.98); }
        .btn-primary:disabled { background: #B0BEC5; cursor: not-allowed; transform: none; }

        .btn-success {
            background: #168821;
            color: #fff;
            border: none;
            padding: 11px 24px;
            border-radius: 100px;
            font-size: 14px;
            font-family: 'Raleway', sans-serif;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: background 0.15s;
        }

        .btn-success:hover { background: #0C5218; }
        .btn-success:disabled { background: #B0BEC5; cursor: not-allowed; }

        .btn-sm {
            padding: 8px 16px;
            font-size: 12px;
        }

        /* ── LOADING ── */
        #loadingMessage {
            display: none;
            justify-content: center;
            align-items: center;
            gap: 12px;
            font-weight: 600;
            color: #1351B4;
            padding: 20px;
            font-size: 14px;
        }

        .spinner {
            width: 22px;
            height: 22px;
            border: 3px solid #C5D4FF;
            border-top-color: #1351B4;
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
        }

        @keyframes spin { to { transform: rotate(360deg); } }

        /* ── TOAST ── */
        .toast {
            visibility: hidden;
            min-width: 260px;
            background: #333;
            color: #fff;
            text-align: center;
            border-radius: 6px;
            padding: 14px 20px;
            position: fixed;
            z-index: 9999;
            left: 50%;
            bottom: 80px;
            transform: translateX(-50%);
            font-size: 13px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.2);
        }

        .toast.show {
            visibility: visible;
            animation: toastIn 0.4s, toastOut 0.4s 2.6s;
        }

        @keyframes toastIn { from { bottom: 60px; opacity: 0; } to { bottom: 80px; opacity: 1; } }
        @keyframes toastOut { from { bottom: 80px; opacity: 1; } to { bottom: 60px; opacity: 0; } }

        /* ── TAB PANELS ── */
        .tab { display: none; }
        .tab.active { display: block; animation: fadeIn 0.3s ease; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }

        /* ── FOOTER ── */
        .site-footer {
            background: #071D41;
            padding: 14px 24px;
            position: fixed;
            bottom: 0;
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 8px;
            z-index: 50;
        }

        .footer-brand {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 13px;
            font-weight: 700;
            color: #fff;
        }

        .footer-copy {
            font-size: 11px;
            color: rgba(255,255,255,0.55);
        }

        /* ── RESPONSIVE ── */
        @media (max-width: 640px) {
            #main-content { padding: 16px 12px; }
            .form-card { padding: 18px; }
            .steps-nav { gap: 8px; padding: 14px 10px; }
            .step-label { display: none; }
            .gov-bar-links { display: none; }
            .page-header { flex-direction: column; align-items: flex-start; gap: 10px; }
            .btn-row { flex-direction: column; }
            .btn-primary, .btn-secondary, .btn-success { width: 100%; justify-content: center; }
        }
    </style>
</head>
<body>

    <!-- BARRA DO GOVERNO FEDERAL -->
    <div id="gov-bar">
        <div class="gov-bar-brand">
            <i class="fas fa-globe" style="color:#fff;"></i>
            GOV.BR
        </div>
        <ul class="gov-bar-links">
            <li><a href="https://www.gov.br/pt-br/orgaos-do-governo">Órgãos do Governo</a></li>
            <li><a href="http://www.acessoainformacao.gov.br/">Acesso à Informação</a></li>
            <li><a href="http://www4.planalto.gov.br/legislacao">Legislação</a></li>
            <li><a href="https://www.gov.br/governodigital/pt-br/acessibilidade-digital">Acessibilidade</a></li>
        </ul>
    </div>

    <!-- HEADER -->
    <header class="ministry-header">
        <div class="header-top-row">
            <div class="header-logo-area">
                <a href="https://www.gov.br" class="govbr-badge" title="Portal GOV.BR">
                    <i class="fas fa-globe"></i> GOV.BR
                </a>
                <div class="divider-v"></div>
                <div class="ministry-name">
                    Ministério do Esporte
                    <small>Gerador de Declarações</small>
                </div>
            </div>
            <div class="header-actions">
                <button class="icon-btn" aria-label="Buscar">
                    <i class="fas fa-search"></i>
                </button>
                <button class="icon-btn" aria-label="Menu">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
        <div class="header-bottom-row">
            <i class="fas fa-home" style="font-size:11px;"></i>
            Gerador de Declarações para Convênio — Ministério do Esporte
        </div>
    </header>

    <!-- BREADCRUMB -->
    <nav class="breadcrumb-bar" aria-label="Você está em:">
        <a href="https://www.gov.br">gov.br</a>
        <span class="sep">›</span>
        <a href="https://www.gov.br/esporte">Ministério do Esporte</a>
        <span class="sep">›</span>
        <a href="#">Portal de Formulários</a>
        <span class="sep">›</span>
        <span class="current">Declaração de Entidade Pública</span>
    </nav>

    <!-- CONTEÚDO PRINCIPAL -->
    <main id="main-content">

        <!-- Cabeçalho da página -->
        <div class="page-header">
            <div class="page-header-left">
                <div class="page-header-icon">
                    <i class="fas fa-file-contract"></i>
                </div>
                <div>
                    <h2>Declaração de Entidade Pública</h2>
                    <p>Preencha os dados abaixo para gerar as declarações do convênio</p>
                </div>
            </div>
            <span class="status-badge"><i class="fas fa-circle" style="font-size:8px; margin-right:4px;"></i> Emissão</span>
        </div>

        <!-- Navegação por etapas -->
        <div class="steps-nav" role="tablist">
            <div class="step-item active" id="step1">
                <button class="step-btn" role="tab" aria-selected="true" aria-controls="aba1"
                        onclick="mostrarAba('aba1')" title="Dados do Dirigente">
                    <i class="fas fa-user-tie"></i>
                </button>
                <span class="step-label">Dirigente</span>
            </div>
            <div class="step-item" id="step2">
                <button class="step-btn" role="tab" aria-selected="false" aria-controls="aba2"
                        onclick="mostrarAba('aba2')" title="Dados da Entidade" disabled>
                    <i class="fas fa-building"></i>
                </button>
                <span class="step-label">Entidade</span>
            </div>
            <div class="step-item" id="step3">
                <button class="step-btn" role="tab" aria-selected="false" aria-controls="aba3"
                        onclick="mostrarAba('aba3')" title="Opções de Geração" disabled>
                    <i class="fas fa-file-pdf"></i>
                </button>
                <span class="step-label">Geração</span>
            </div>
        </div>

        <!-- FORMULÁRIO -->
        <form id="declaracoesForm" onsubmit="return false;" aria-label="Formulário de Declaração">

            <!-- ABA 1 - DADOS DO DIRIGENTE -->
            <div id="aba1" class="tab active" role="tabpanel">
                <div class="form-card">
                    <div class="section-title">
                        <i class="fas fa-user-edit"></i>
                        Dados do Dirigente do Convênio
                    </div>

                    <div class="form-row">
                        <div class="half-width">
                            <label class="field-label" for="dirigente">Nome completo</label>
                            <input type="text" id="dirigente" class="govbr-input"
                                   oninput="validarLetras(this)" required
                                   aria-describedby="erro-dirigente"
                                   placeholder="Ex: João da Silva">
                            <span id="erro-dirigente" class="error-message">Digite apenas letras e espaços.</span>
                        </div>
                        <div class="half-width">
                            <label class="field-label" for="matricula">Matrícula Funcional</label>
                            <input type="text" id="matricula" class="govbr-input"
                                   oninput="validarNumeros(this)" required
                                   aria-describedby="erro-matricula"
                                   placeholder="Ex: 00123456 ou 00123456-7">
                            <span id="erro-matricula" class="error-message">Digite apenas números.</span>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="half-width">
                            <label class="field-label" for="cargoDirigente">Cargo do Dirigente</label>
                            <input type="text" id="cargoDirigente" class="govbr-input"
                                   placeholder="Ex: Prefeito(a), Secretário(a)"
                                   onkeypress="return validarLetrasApenas(event)"
                                   oninput="validarLetras(this)" required
                                   aria-describedby="erro-cargoDirigente">
                            <span id="erro-cargoDirigente" class="error-message">Digite apenas letras e espaços.</span>
                        </div>
                        <div class="half-width">
                            <label class="field-label" for="proposta">Proposta (XXXXXX/ANO)</label>
                            <input type="text" id="proposta" class="govbr-input"
                                   oninput="validarProposta(this)" maxlength="11"
                                   placeholder="Ex: 000123/2025" required
                                   aria-describedby="erro-proposta">
                            <span id="erro-proposta" class="error-message">Formato: XXXXXX/ANO (6 dígitos/ano).</span>
                        </div>
                    </div>
                </div>

                <div class="btn-row">
                    <button type="button" class="btn-primary" onclick="validarAba('aba1', 'aba2')">
                        Próxima etapa <i class="fas fa-arrow-right"></i>
                    </button>
                </div>
            </div>

            <!-- ABA 2 - DADOS DA ENTIDADE -->
            <div id="aba2" class="tab" role="tabpanel">
                <div class="form-card">
                    <div class="section-title">
                        <i class="fas fa-id-card"></i>
                        Dados da Entidade
                    </div>

                    <div class="form-row">
                        <div class="half-width">
                            <label class="field-label" for="cnpj">CNPJ</label>
                            <input type="text" id="cnpj" class="govbr-input"
                                   maxlength="18" oninput="mascaraCNPJ(this)"
                                   placeholder="00.000.000/0000-00" required
                                   aria-describedby="erro-cnpj">
                            <span id="erro-cnpj" class="error-message">CNPJ inválido ou não encontrado.</span>
                        </div>
                        <div class="half-width">
                            <label class="field-label" for="entidade">Nome da Entidade</label>
                            <input type="text" id="entidade" class="govbr-input" readonly required
                                   aria-describedby="erro-entidade"
                                   placeholder="Preenchido automaticamente">
                            <span id="erro-entidade" class="error-message">Nome da entidade não preenchido.</span>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="full-width">
                            <label class="field-label" for="endereco">Endereço Completo</label>
                            <input type="text" id="endereco" class="govbr-input" readonly required
                                   aria-describedby="erro-endereco"
                                   placeholder="Preenchido automaticamente">
                            <span id="erro-endereco" class="error-message">Endereço não preenchido.</span>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="third-width">
                            <label class="field-label" for="uf">UF</label>
                            <input type="text" id="uf" class="govbr-input" readonly required
                                   aria-describedby="erro-uf"
                                   placeholder="--">
                            <span id="erro-uf" class="error-message">UF não preenchida.</span>
                        </div>
                        <div class="third-width">
                            <label class="field-label" for="municipio">Município</label>
                            <input type="text" id="municipio" class="govbr-input" readonly required
                                   aria-describedby="erro-municipio"
                                   placeholder="Preenchido automaticamente">
                            <span id="erro-municipio" class="error-message">Município não preenchido.</span>
                        </div>
                        <div class="third-width">
                            <label class="field-label" for="cep">CEP</label>
                            <input type="text" id="cep" class="govbr-input" readonly required
                                   aria-describedby="erro-cep"
                                   placeholder="00000-000">
                            <span id="erro-cep" class="error-message">CEP não preenchido.</span>
                        </div>
                    </div>
                </div>

                <div class="btn-row">
                    <button type="button" class="btn-secondary" onclick="mostrarAba('aba1')">
                        <i class="fas fa-arrow-left"></i> Voltar
                    </button>
                    <button type="button" class="btn-primary" onclick="validarAba('aba2', 'aba3')">
                        Próxima etapa <i class="fas fa-arrow-right"></i>
                    </button>
                </div>
            </div>

            <!-- ABA 3 - OPÇÕES DE GERAÇÃO -->
            <div id="aba3" class="tab" role="tabpanel">
                <div class="form-card">
                    <div class="section-title">
                        <i class="fas fa-cogs"></i>
                        Escolha uma Opção para Gerar o PDF
                    </div>

                    <!-- Ação 00SL -->
                    <div class="option-group" id="optGroup00SL">
                        <label for="radio_00SL" class="option-group-header">
                            <input type="radio" id="radio_00SL" name="tipo_proposta"
                                   value="00SL" onchange="atualizarOpcao()">
                            <i class="fas fa-layer-group" style="color:#1351B4;"></i>
                            Ação 00SL
                        </label>
                        <div id="sub_options_00SL" class="sub-options">
                            <p><i class="fas fa-coins" style="margin-right:4px;"></i> Origem do Recurso</p>
                            <div class="radio-container">
                                <label class="radio-option" id="opt00SL_emendas">
                                    <input type="radio" id="radio_00SL_emenda" name="origem_00SL"
                                           value="emendas" onchange="atualizarOpcao(); atualizarRadioStyle()">
                                    Emenda Individual e Bancada
                                </label>
                                <label class="radio-option" id="opt00SL_comissao">
                                    <input type="radio" id="radio_00SL_comissao" name="origem_00SL"
                                           value="comissao" onchange="atualizarOpcao(); atualizarRadioStyle()">
                                    Discricionário e Comissão
                                </label>
                            </div>

                            <div class="form-row" id="municipioMais65milContainer00SL" style="display:none; margin-top:12px;">
                                <div class="full-width">
                                    <label class="checkbox-wrapper">
                                        <input type="checkbox" id="municipioMais65mil00SL" onchange="atualizarOpcao()">
                                        <span>Município tem mais de 65 mil habitantes</span>
                                    </label>
                                </div>
                            </div>

                            <div id="camposEspacoFisico" class="espaco-fisico-container" style="display:none;">
                                <h4><i class="fas fa-map-marker-alt" style="margin-right:6px;"></i>Dados do Espaço Físico</h4>
                                <div id="espacoFisicoFields">
                                    <div class="form-row" id="espacoFisicoRow0">
                                        <div class="half-width">
                                            <label class="field-label" for="nomeEspacoFisico0">Nome do Espaço Físico</label>
                                            <input type="text" id="nomeEspacoFisico0" class="govbr-input"
                                                   oninput="validarLetras(this)" required
                                                   aria-describedby="erro-nomeEspacoFisico0"
                                                   placeholder="Ex: Ginásio Municipal">
                                            <span id="erro-nomeEspacoFisico0" class="error-message">Campo obrigatório.</span>
                                        </div>
                                        <div class="half-width">
                                            <label class="field-label" for="enderecoEspacoFisico0">Endereço do Espaço Físico</label>
                                            <input type="text" id="enderecoEspacoFisico0" class="govbr-input" required
                                                   aria-describedby="erro-enderecoEspacoFisico0"
                                                   placeholder="Ex: Rua das Flores, 100">
                                            <span id="erro-enderecoEspacoFisico0" class="error-message">Campo obrigatório.</span>
                                        </div>
                                        <button type="button" class="remove-btn" onclick="removerEspacoFisico(0)" style="display:none;">
                                            <i class="fas fa-trash"></i> Remover
                                        </button>
                                    </div>
                                </div>
                                <div style="display:flex; gap:10px; margin-top:14px;">
                                    <button type="button" class="btn-secondary btn-sm" onclick="adicionarEspacoFisico()">
                                        <i class="fas fa-plus"></i> Adicionar Espaço
                                    </button>
                                    <button type="button" class="btn-primary btn-sm" onclick="salvarEspacoFisico()">
                                        <i class="fas fa-save"></i> Salvar
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Ação 20JP -->
                    <div class="option-group" id="optGroup20JP">
                        <label for="radio_20JP" class="option-group-header">
                            <input type="radio" id="radio_20JP" name="tipo_proposta"
                                   value="20JP" onchange="atualizarOpcao()">
                            <i class="fas fa-layer-group" style="color:#1351B4;"></i>
                            Ação 20JP
                        </label>
                        <div id="sub_options_20JP" class="sub-options">
                            <p><i class="fas fa-coins" style="margin-right:4px;"></i> Origem do Recurso</p>
                            <div class="radio-container">
                                <label class="radio-option" id="opt20JP_emenda">
                                    <input type="radio" id="radio_20JP_emenda" name="origem_20JP"
                                           value="emenda" onchange="atualizarOpcao(); atualizarRadioStyle()">
                                    Emenda Individual e de Bancada
                                </label>
                                <label class="radio-option" id="opt20JP_comissao">
                                    <input type="radio" id="radio_20JP_comissao" name="origem_20JP"
                                           value="comissao" onchange="atualizarOpcao(); atualizarRadioStyle()">
                                    Discricionário e Comissão
                                </label>
                            </div>

                            <div class="form-row" id="municipioMais65milContainer20JP" style="display:none; margin-top:12px;">
                                <div class="full-width">
                                    <label class="checkbox-wrapper">
                                        <input type="checkbox" id="municipioMais65mil20JP" onchange="atualizarOpcao()">
                                        <span>Município tem mais de 65 mil habitantes</span>
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

                <div id="loadingMessage">
                    <div class="spinner"></div>
                    Processando PDF...
                </div>

                <div class="btn-row">
                    <button type="button" class="btn-secondary" onclick="mostrarAba('aba2')">
                        <i class="fas fa-arrow-left"></i> Voltar
                    </button>
                    <button type="button" id="gerarPDF" class="btn-success" disabled>
                        <i class="fas fa-file-pdf"></i> Gerar PDF
                    </button>
                </div>
            </div>

        </form>

        <div id="toast" class="toast"></div>

    </main>

    <!-- FOOTER -->
    <footer class="site-footer">
        <div class="footer-brand">
            <i class="fas fa-globe"></i>
            GOV.BR — Ministério do Esporte
        </div>
        <span class="footer-copy">© 2026 Ministério do Esporte — Governo Federal do Brasil</span>
    </footer>

    <!-- Scripts PDF -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.16.105/pdf.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/pdfmake.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/vfs_fonts.js"></script>

    <script>
    // ═══════════════════════════════════════════════════════════
    // CONFIGURAÇÃO DE FONTES
    // ═══════════════════════════════════════════════════════════
    pdfMake.fonts = {
        Roboto: {
            normal: 'https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/fonts/Roboto/Roboto-Regular.ttf',
            bold: 'https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/fonts/Roboto/Roboto-Medium.ttf',
            italics: 'https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/fonts/Roboto/Roboto-Italic.ttf',
            bolditalics: 'https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/fonts/Roboto/Roboto-MediumItalic.ttf'
        }
    };

    // ═══════════════════════════════════════════════════════════
    // ESTADO GLOBAL
    // ═══════════════════════════════════════════════════════════
    let espacoFisicoCount = 1;
    let isGeneratingPDF = false;

    // ═══════════════════════════════════════════════════════════
    // DECLARAÇÕES COMPLETAS (conteúdo preservado integralmente)
    // ═══════════════════════════════════════════════════════════
    const declaracoesCompletas = [
        {
            title: "DECLARAÇÃO DE AUSÊNCIA DE DESTINAÇÃO DE RECURSOS",
            content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], declaro, sob as penas da lei, que os recursos oriundos do presente convênio, celebrado com o Ministério do Esporte - MESP, sob o número da Proposta nº [proposta], não se destinarão para o pagamento de despesas com pessoal ativo, inativo ou pensionista, dos Estados, do Distrito Federal e dos Municípios, conforme disposto no Art. 167, inciso X, da Constituição Federal de 1988 (CF/88) e no Art. 25, § 1º, inciso III, da Lei Complementar nº 101/2000 (Lei de Responsabilidade Fiscal). Ressalto que esta declaração visa assegurar a correta aplicação dos recursos públicos, estando ciente das sanções administrativas, civis e penais aplicáveis em caso de descumprimento.`
        },
        {
            title: "DECLARAÇÃO DE NÃO VÍNCULO",
            content: `Eu, [dirigente], matrícula [matricula], cargo [cargoDirigente], declaro, sob as penas da lei, especialmente o art. 299 do Código Penal Brasileiro, que as Empresas a serem contratadas no âmbito do Convênio a ser celebrado com o Ministério do Esporte - MESP, sob o número da Proposta nº [proposta], não possuem em seu quadro societário, cônjuge ou companheiro, bem como vínculo de parentesco, colateral ou por afinidade até o terceiro grau, ou de natureza técnica, comercial, econômica, financeira, trabalhista e civil, com qualquer dirigente, servidor ou empregado da [entidade] ou do Ministério do Esporte. Esta declaração é prestada com base em pesquisa documental e está sujeita a verificação, sendo de minha responsabilidade a veracidade das informações fornecidas.`
        },
        {
            title: "DECLARAÇÃO NEGATIVA DE DUPLICIDADE DE CONVÊNIO",
            content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], declaro, para os devidos fins de celebração de convênios junto ao Ministério do Esporte - MESP, que a proposta inserida no Sistema Eletrônico Transferegov sob o nº [proposta], juntamente com todas as informações e documentações anexas, foi apresentada para apreciação exclusivamente junto a esse órgão e em nenhum outro ente da administração pública federal, estadual ou municipal. Esta declaração visa evitar duplicidade de financiamentos e está sujeita às sanções civis, administrativas e penais cabíveis, nos termos da Lei nº 8.666/1993 e da Lei nº 14.133/2021 (Nova Lei de Licitações), caso seja comprovada falsidade ideológica ou omissão de informações.`
        },
        {
            title: "DECLARAÇÃO NÃO RECEBE RECURSOS DE OUTRA ENTIDADE PARA A MESMA FINALIDADE",
            content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], DECLARO ao Ministério do Esporte - MESP que a entidade a qual represento não recebe, atualmente ou no período de vigência da Proposta nº [proposta], recursos financeiros de outra entidade pública ou privada para a mesma finalidade das ações apresentadas e especificadas no Plano de Trabalho cadastrado no Sistema Eletrônico Transferegov. Esta afirmação tem como objetivo evitar sobreposição de recursos e garantir a economicidade dos fundos públicos, estando ciente das penalidades previstas em caso de descumprimento, conforme a legislação aplicável.`
        },
        {
            title: "DECLARAÇÃO NÃO CONTRATAÇÃO COM RECURSOS DA PARCERIA",
            content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal da [entidade], CNPJ nº [cnpj], declaro, para os devidos fins de celebração do Termo de Convênio com o Ministério do Esporte - MESP, que a presente Entidade não contratará, com os recursos provenientes desta parceria sob a Proposta nº [proposta], empresas que: (i) integrem o mesmo grupo econômico; (ii) possuam participação societária cruzada; (iii) pertençam ou tenham participação societária de parentes de dirigentes ou funcionários da [entidade]; (iv) compartilhem o mesmo endereço, telefone ou CNPJ; (v) apresentem incompatibilidade entre a classificação de atividades econômicas (CNAE) e o objeto contratado. Além disso, asseguro que as cotações dos itens previstos no Plano de Trabalho foram obtidas de forma transparente, com documentos comprobatórios arquivados, e que me responsabilizo pela veracidade e conformidade das pesquisas de preços junto aos fornecedores, nos termos da Instrução Normativa SEGES/ME nº 65/2021.`
        },
        {
            title: "DECLARAÇÃO DE COMPROMISSO",
            content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], declaro o compromisso de dispor dos recursos informatizados necessários ao acesso ao Sistema Eletrônico Transferegov, com o objetivo de alimentar, atualizar e acompanhar de forma permanente o referido sistema, de acordo com a norma vigente, durante todo o período da formalização da parceria até prestação de contas final;`
        },
        {
            title: "DECLARAÇÃO DE CUSTOS",
            content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], ATESTO a planilha de custos e as cotações de preços obtidas, elaboradas em conformidade com a Instrução Normativa SEGES/ME nº 65, de 7 de julho de 2021, e inseridas no Sistema Eletrônico Transferegov sob a Proposta nº [proposta]. Declaro que os valores apresentados foram apurados com base em pesquisa de mercado realizada junto a, no mínimo, três fornecedores, refletindo preços praticados no setor e compatíveis com a natureza dos bens e serviços contratados. Ressalto que os documentos comprobatórios, incluindo orçamentos e cotações, estão arquivados e disponíveis para fiscalização, e que me responsabilizo pela veracidade e adequação dos dados apresentados, sob pena de sanções administrativas e legais aplicáveis.`
        },
        {
            title: "DECLARAÇÃO DE CIÊNCIA QUANTO À VEDAÇÃO DE SUBCONTRATAÇÃO INTEGRAL DO OBJETO",
            content: `Eu, [dirigente], matrícula [matricula], na condição de [cargoDirigente] do(a) [entidade], CNPJ nº [cnpj], declaro, para os devidos fins, ter ciência das disposições constantes na Portaria Conjunta MGI/MF/CGU nº 33, de 30 de agosto de 2023, bem como das demais normas aplicáveis à celebração e execução de convênios, especialmente quanto à vedação de subcontratação integral da execução do objeto pactuado.\n\nDeclaro, ainda, estar ciente de que a transferência integral da execução do objeto a terceiros descaracteriza a atuação do convenente na implementação da política pública pactuada, podendo afrontar os princípios da finalidade pública, da eficiência, da responsabilidade administrativa e do interesse público.\n\nNesse sentido, este ente público, por possuir capacidade técnica e operacional compatível com o objeto proposto, compromete-se a:\n\nI – não promover a subcontratação integral da execução do objeto do convênio;\n\nII – assegurar a participação ativa do convenente na execução, acompanhamento e fiscalização das ações pactuadas;\n\nIII – indicar responsáveis pela Coordenação-Geral, acompanhamento e execução do Projeto e/ou Evento, como forma de assunção das responsabilidades inerentes à parceria e garantia da adequada implementação da política pública objeto do instrumento;\n\nIV – contratar somente Recursos Humanos cuja função não exista na estrutura do Município.\n\nPor ser expressão da verdade, firmo a presente declaração para que produza os efeitos legais cabíveis.`
        },
        {
            title: "DECLARAÇÃO DE CIÊNCIA DAS CONDUTAS VEDADAS EM ANO ELEITORAL",
            content: `Eu, [dirigente], matrícula nº [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], declaro, para fins de celebração de convênio com o Ministério do Esporte, que tenho ciência das condutas vedadas aos agentes públicos em ano eleitoral, previstas na legislação vigente, em especial no que se refere à transferência voluntária de recursos públicos, disposta no art. 73, inciso VI, alínea "a", da Lei nº 9.504, de 30 de setembro de 1997, a qual prevê que a União está impedida de realizar transferência voluntária de recursos aos Estados e Municípios, sob pena de nulidade de pleno direito, ressalvados os recursos destinados a cumprir obrigação formal preexistente para execução de obra ou serviço em andamento e com cronograma prefixado, e os destinados a atender situações de emergência e de calamidade pública, nos três meses anteriores à eleição.\n\n            Declaro, ainda, que tenho conhecimento de que o descumprimento das vedações eleitorais pode acarretar a nulidade dos atos praticados e a responsabilização dos agentes envolvidos.\n\n            Comprometo-me a observar integralmente a legislação eleitoral e os normativos aplicáveis às transferências voluntárias da União, abstendo-me de praticar atos que possam configurar infração às referidas normas.\n\n            Por ser expressão da verdade, firmo a presente declaração.\n\n            [municipio]/[uf], [diaAtual] de [mesAtual] de [anoAtual]`
        }
    ];

    const declaracoesEspecificas = {
        '00SL_emendas': [
            {
                title: "DECLARAÇÃO DE SUSTENTABILIDADE DO OBJETO",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], DECLARO perante o Ministério do Esporte, para fins de celebração de convênio sob a Proposta nº [proposta], que o(a) [entidade] possui condições orçamentárias e financeiras para arcar com as despesas decorrentes da execução do objeto, incluindo custos de manutenção, operação e eventuais contingências, garantindo a sustentabilidade do projeto ao longo de sua vigência. Esta declaração considera a aquisição de bens de capital e está respaldada por planejamento orçamentário documentado, estando ciente das responsabilidades previstas na Lei nº 4.320/1964 e na Lei Complementar nº 101/2000.`
            },
            {
                title: "DECLARAÇÃO DE TITULARIDADE DO TERRENO",
                content: `Eu, [dirigente], matrícula nº [matricula], na condição de representante legal do(a) [entidade], inscrito no CNPJ nº [cnpj], declaro que o terreno destinado à execução do objeto da Proposta nº [proposta], é de domínio público e pertence ao Município de [municipio]/[uf], estando plenamente disponível, apto e compatível para a instalação dos equipamentos previstos.\n\nEsta declaração é prestada com base na documentação oficial arquivada, comprometendo-me a apresentar os títulos de propriedade, matrículas ou atos administrativos comprobatórios, caso solicitados, nos termos da Lei nº 14.133/2021.`
            },
            {
                title: "DECLARAÇÃO DE CONFORMIDADE EM ACESSIBILIDADE",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], DECLARO que serão garantidos os meios necessários para assegurar a acessibilidade de pessoas com deficiência ou com mobilidade reduzida ao projeto objeto da Proposta nº [proposta], em conformidade com a Lei nº 10.098, de 19 de dezembro de 2000, o Decreto nº 5.296/2004 e a Norma Brasileira de Acessibilidade (NBR 9050). Comprometo-me a implementar rampas, sinalizações táteis, banheiros adaptados e demais adaptações exigidas, estando ciente de que a não conformidade sujeitará a [entidade] às sanções previstas na legislação. Declaro, outrossim, sob as penas da lei, deter plenos poderes e informações para firmar esta declaração.`
            },
            {
                title: "DECLARAÇÃO DE CUSTEIO DA INSTALAÇÃO DOS EQUIPAMENTOS",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], declaro o compromisso de dispor de recursos financeiros próprios ou de outras fontes lícitas, não vinculados à Proposta nº [proposta], para custear integralmente a instalação dos equipamentos pactuados, incluindo mão de obra, materiais complementares, serviços necessários à implantação, adequação da área de instalação e eventuais despesas imprevistas.\n\nDeclaro, ainda, que a responsabilidade pela instalação e pelo pleno funcionamento dos equipamentos é integralmente do proponente, inclusive quanto à execução e ao custeio de eventuais obras, serviços de engenharia ou adequações que se mostrem necessários à sua implantação e operação.\n\nComprometo-me a não utilizar os recursos transferidos por meio do instrumento para custear obras, serviços ou despesas de instalação não abrangidos pelo objeto pactuado, responsabilizando-me pela adoção de todas as providências necessárias para assegurar a adequada implantação e funcionamento dos equipamentos.\n\nEsta declaração está respaldada por planejamento orçamentário documentado, e me responsabilizo por garantir a execução das ações necessárias à instalação dos equipamentos nos prazos estabelecidos.`
            }
        ],
        '00SL_comissao': [
            {
                title: "DECLARAÇÃO DE SUSTENTABILIDADE DO OBJETO",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], DECLARO perante o Ministério do Esporte, para fins de celebração de convênio sob a Proposta nº [proposta], que o(a) [entidade] possui condições orçamentárias e financeiras para arcar com as despesas decorrentes da execução do objeto, incluindo custos de manutenção, operação e eventuais contingências, garantindo a sustentabilidade do projeto ao longo de sua vigência. Esta declaração considera a aquisição de bens de capital e está respaldada por planejamento orçamentário documentado, estando ciente das responsabilidades previstas na Lei nº 4.320/1964 e na Lei Complementar nº 101/2000.`
            },
            {
                title: "DECLARAÇÃO DE TITULARIDADE DO TERRENO",
                content: `Eu, [dirigente], matrícula nº [matricula], na condição de representante legal do(a) [entidade], inscrito no CNPJ nº [cnpj], declaro que o terreno destinado à execução do objeto da Proposta nº [proposta], é de domínio público e pertence ao Município de [municipio]/[uf], estando plenamente disponível, apto e compatível para a instalação dos equipamentos previstos.\n\nEsta declaração é prestada com base na documentação oficial arquivada, comprometendo-me a apresentar os títulos de propriedade, matrículas ou atos administrativos comprobatórios, caso solicitados, nos termos da Lei nº 14.133/2021.`
            },
            {
                title: "DECLARAÇÃO DE CONFORMIDADE EM ACESSIBILIDADE",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], DECLARO que serão garantidos os meios necessários para assegurar a acessibilidade de pessoas com deficiência ou com mobilidade reduzida ao projeto objeto da Proposta nº [proposta], em conformidade com a Lei nº 10.098, de 19 de dezembro de 2000, o Decreto nº 5.296/2004 e a Norma Brasileira de Acessibilidade (NBR 9050). Comprometo-me a implementar rampas, sinalizações táteis, banheiros adaptados e demais adaptações exigidas, estando ciente de que a não conformidade sujeitará a [entidade] às sanções previstas na legislação. Declaro, outrossim, sob as penas da lei, deter plenos poderes e informações para firmar esta declaração.`
            },
            {
                title: "DECLARAÇÃO DE CUSTEIO DA INSTALAÇÃO DOS EQUIPAMENTOS",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], declaro o compromisso de dispor de recursos financeiros próprios ou de outras fontes lícitas, não vinculados à Proposta nº [proposta], para custear integralmente a instalação dos equipamentos pactuados, incluindo mão de obra, materiais complementares, serviços necessários à implantação, adequação da área de instalação e eventuais despesas imprevistas.\n\nDeclaro, ainda, que a responsabilidade pela instalação e pelo pleno funcionamento dos equipamentos é integralmente do proponente, inclusive quanto à execução e ao custeio de eventuais obras, serviços de engenharia ou adequações que se mostrem necessários à sua implantação e operação.\n\nComprometo-me a não utilizar os recursos transferidos por meio do instrumento para custear obras, serviços ou despesas de instalação não abrangidos pelo objeto pactuado, responsabilizando-me pela adoção de todas as providências necessárias para assegurar a adequada implantação e funcionamento dos equipamentos.\n\nEsta declaração está respaldada por planejamento orçamentário documentado, e me responsabilizo por garantir a execução das ações necessárias à instalação dos equipamentos nos prazos estabelecidos.`
            }
        ],
        '20JP_emenda': [
            {
                title: "DECLARAÇÃO DE CIÊNCIA DOS REQUISITOS PARA CONTRATAÇÃO DE RECURSOS HUMANOS",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], no que diz respeito à contratação de recursos humanos para a execução da Proposta nº [proposta], declaro ter ciência de que:\n\n1. A forma de contratação deverá ser analisada e aprovada pela Consultoria Jurídica da Entidade Convenente, observando as orientações contidas no Acórdão nº 2588/2017 – TCU – Plenário, na Portaria Conjunta MGI/MF/AGU nº 33, de 30 de agosto de 2023, e na Lei nº 14.133/2021 (Nova Lei de Licitações).\n\n2. O repasse de recursos financeiros para custeio desta ação, incluindo pagamento de profissionais e encargos sociais/trabalhistas, seguirá os valores e percentuais aprovados no Plano de Trabalho da Proposta nº [proposta]. Caso os encargos sociais ou trabalhistas excedam o limite estipulado, a [entidade] arcará com o diferencial, sem ônus para o convenente.\n\n3. O pagamento será realizado mensalmente, conforme pactuado, observadas as seguintes condições:\n   - Pagamento dos Profissionais: efetuado no mês subsequente à prestação dos serviços, mediante comprovação documental;\n   - Pagamento dos Encargos Sociais e/ou Trabalhistas: acompanhará a periodicidade dos pagamentos aos recursos humanos vinculados, com recolhimento tempestivo aos órgãos competentes.\n\nDeclaro estar ciente de que o descumprimento destas condições poderá acarretar a suspensão do repasse de recursos e a aplicação de sanções administrativas.`
            }
        ],
        '20JP_comissao': [
            {
                title: "DECLARAÇÃO DE CIÊNCIA DOS REQUISITOS PARA CONTRATAÇÃO DE RECURSOS HUMANOS",
                content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], no que diz respeito à contratação de recursos humanos para a execução da Proposta nº [proposta], declaro ter ciência de que:\n\n1. A forma de contratação deverá ser analisada e aprovada pela Consultoria Jurídica da Entidade Convenente, observando as orientações contidas no Acórdão nº 2588/2017 – TCU – Plenário, na Portaria Conjunta MGI/MF/AGU nº 33, de 30 de agosto de 2023, e na Lei nº 14.133/2021 (Nova Lei de Licitações).\n\n2. O repasse de recursos financeiros para custeio desta ação, incluindo pagamento de profissionais e encargos sociais/trabalhistas, seguirá os valores e percentuais aprovados no Plano de Trabalho da Proposta nº [proposta]. Caso os encargos sociais ou trabalhistas excedam o limite estipulado, a [entidade] arcará com o diferencial, sem ônus para o convenente.\n\n3. O pagamento será realizado mensalmente, conforme pactuado, observadas as seguintes condições:\n   - Pagamento dos Profissionais: efetuado no mês subsequente à prestação dos serviços, mediante comprovação documental;\n   - Pagamento dos Encargos Sociais e/ou Trabalhistas: acompanhará a periodicidade dos pagamentos aos recursos humanos vinculados, com recolhimento tempestivo aos órgãos competentes.\n\nDeclaro estar ciente de que o descumprimento destas condições poderá acarretar a suspensão do repasse de recursos e a aplicação de sanções administrativas.`
            }
        ]
    };

    // ═══════════════════════════════════════════════════════════
    // FUNÇÕES AUXILIARES (preservadas integralmente)
    // ═══════════════════════════════════════════════════════════
    async function getBase64ImageFromUrl(imageUrl) {
        if (!imageUrl) return null;
        try {
            const response = await fetch(imageUrl);
            if (!response.ok) throw new Error(`Failed to load image: ${response.statusText}`);
            const blob = await response.blob();
            return new Promise((resolve, reject) => {
                const reader = new FileReader();
                reader.onloadend = () => resolve(reader.result);
                reader.onerror = reject;
                reader.readAsDataURL(blob);
            });
        } catch (error) {
            console.error("Error in getBase64ImageFromUrl:", error);
            return null;
        }
    }

    function substituirPlaceholders(texto, dados) {
        const dataAtual = new Date();
        return texto
            .replace(/\[dirigente]/g, dados.dirigente || 'Nome não informado')
            .replace(/\[matricula]/g, dados.matricula || 'Matrícula não informada')
            .replace(/\[cargoDirigente]/g, dados.cargoDirigente || 'Cargo não informado')
            .replace(/\[entidade]/g, dados.entidade || 'Entidade não informada')
            .replace(/\[cnpj]/g, dados.cnpj || 'CNPJ não informado')
            .replace(/\[endereco]/g, dados.endereco || 'Endereço não informado')
            .replace(/\[uf]/g, dados.uf || 'UF não informada')
            .replace(/\[municipio]/g, dados.municipio || 'Município não informado')
            .replace(/\[cep]/g, dados.cep || 'CEP não informado')
            .replace(/\[proposta]/g, dados.proposta || 'Proposta não informada')
            .replace(/\[diaAtual]/g, String(dataAtual.getDate()).padStart(2, '0'))
            .replace(/\[mesAtual]/g, dataAtual.toLocaleString('pt-BR', { month: 'long' }))
            .replace(/\[anoAtual]/g, dataAtual.getFullYear())
            .replace(/\[nomeEspacoFisico]/g, dados.espacosFisicos[0]?.nome || 'Nome não informado')
            .replace(/\[enderecoEspacoFisico]/g, dados.espacosFisicos[0]?.endereco || 'Endereço não informado');
    }

    function numeroParaExtenso(num) {
        const units = ['', 'um', 'dois', 'três', 'quatro', 'cinco', 'seis', 'sete', 'oito', 'nove', 'dez', 'onze', 'doze', 'treze', 'catorze', 'quinze', 'dezesseis', 'dezessete', 'dezoito', 'dezenove'];
        const tens = ['', '', 'vinte', 'trinta', 'quarenta', 'cinquenta', 'sessenta', 'setenta', 'oitenta', 'noventa'];
        if (num < 20) return units[num];
        if (num < 100) return tens[Math.floor(num / 10)] + (num % 10 ? ' e ' + units[num % 10] : '');
        return String(num);
    }

    function atualizarOpcao() {
        const tipoProposta = document.querySelector('input[name="tipo_proposta"]:checked')?.value || '';
        const origem00SL = document.querySelector('input[name="origem_00SL"]:checked')?.value || '';
        const origem20JP = document.querySelector('input[name="origem_20JP"]:checked')?.value || '';
        const municipioMais65mil00SL = document.getElementById('municipioMais65mil00SL')?.checked || false;
        const municipioMais65mil20JP = document.getElementById('municipioMais65mil20JP')?.checked || false;

        const municipioMais65milContainer00SL = document.getElementById('municipioMais65milContainer00SL');
        const municipioMais65milContainer20JP = document.getElementById('municipioMais65milContainer20JP');

        if (municipioMais65milContainer00SL) {
            municipioMais65milContainer00SL.style.display =
                (tipoProposta === '00SL' && origem00SL === 'comissao') ? 'block' : 'none';
        }
        if (municipioMais65milContainer20JP) {
            municipioMais65milContainer20JP.style.display =
                (tipoProposta === '20JP' && origem20JP === 'comissao') ? 'block' : 'none';
        }

        // Atualiza visual dos grupos
        document.querySelectorAll('.option-group').forEach(g => g.classList.remove('selected'));
        if (tipoProposta === '00SL') document.getElementById('optGroup00SL').classList.add('selected');
        if (tipoProposta === '20JP') document.getElementById('optGroup20JP').classList.add('selected');

        validarGerarPDF();
    }

    function atualizarRadioStyle() {
        document.querySelectorAll('.radio-option').forEach(opt => {
            const input = opt.querySelector('input[type="radio"]');
            opt.classList.toggle('checked', input && input.checked);
        });
    }

    async function gerarPDF(formData) {
        if (isGeneratingPDF) return;
        isGeneratingPDF = true;
        const loadingMessage = document.getElementById('loadingMessage');
        if (loadingMessage) loadingMessage.style.display = 'flex';

        const dados = {
            ...formData,
            diaAtual: String(new Date().getDate()).padStart(2, '0'),
            mesAtual: new Date().toLocaleString('pt-BR', { month: 'long' }),
            anoAtual: new Date().getFullYear()
        };

        try {
            const erros = validarDadosFormulario(dados);
            if (erros.length > 0) {
                showToast(erros.join(' '), true);
                return;
            }

            const letterheadImage = await getBase64ImageFromUrl('https://i.ibb.co/Q7tqVPBb/Folha-Limpa.jpg');

            const defaults = { leftRightMargin: 40, topMargin: 120, bottomMargin: 100, footerPosY: 770 };
            const layoutOptions = { topMargin: defaults.topMargin, footerPosition: defaults.footerPosY };
            const bottomMargin = Math.max(100, 841.89 - layoutOptions.footerPosition - 40);

            const todasDeclaracoes = new Map();
            declaracoesCompletas.forEach(decl => {
                // ── AJUSTE 00SL: a Declaração de Ciência quanto à Vedação de Subcontratação
                // Integral do Objeto é excluída SOMENTE nas propostas 00SL (mantida na 20JP).
                const ehSubcontratacao = decl.title === "DECLARAÇÃO DE CIÊNCIA QUANTO À VEDAÇÃO DE SUBCONTRATAÇÃO INTEGRAL DO OBJETO";
                if (ehSubcontratacao && dados.opcaoSelecao.startsWith('00SL')) {
                    return; // pula esta declaração nas propostas 00SL
                }

                const ehSustentabilidade = decl.title === "DECLARAÇÃO DE SUSTENTABILIDADE DO OBJETO";
                const condicaoSustentabilidade = dados.opcaoSelecao.startsWith('00SL') || dados.opcaoSelecao.startsWith('20JP');
                if (!ehSustentabilidade || condicaoSustentabilidade) {
                    todasDeclaracoes.set(decl.title, decl);
                }
            });

            const opcaoBase = dados.opcaoSelecao.replace(/_mais65mil/g, '');
            const declaracoesEspecificasArray = declaracoesEspecificas[opcaoBase] || [];
            declaracoesEspecificasArray.forEach(decl => {
                todasDeclaracoes.set(decl.title, decl);
            });

            if ((dados.municipioMais65mil00SL && dados.opcaoSelecao.startsWith('00SL_comissao')) ||
                (dados.municipioMais65mil20JP && dados.opcaoSelecao.startsWith('20JP_comissao'))) {
                const adimplencia = {
                    title: "DECLARAÇÃO DE ADIMPLÊNCIA",
                    content: `Eu, [dirigente], matrícula [matricula], na condição de representante legal do(a) [entidade], CNPJ nº [cnpj], DECLARO, no uso das atribuições que me foram delegadas e sob as penas da lei, que a presente Entidade não está inadimplente com a União, incluindo, mas não se limitando a, contribuições previstas nos artigos 195 e 239 da Constituição Federal (seguridade social, PIS/PASEP e FGTS), bem como obrigações decorrentes de convênios, contratos, acordos, ajustes, subvenções sociais, contribuições e auxílios previamente celebrados com a Administração Pública Federal. Esta declaração está baseada em certidões negativas atualizadas, arquivadas na [entidade], e me comprometo a apresentá-las quando requisitadas, nos termos da Lei nº 14.133/2021.`
                };
                todasDeclaracoes.set(adimplencia.title, adimplencia);
            }

            const declaracoesParaIncluir = Array.from(todasDeclaracoes.values());
            let currentPage = 1;
            const pageNumbers = new Map();

            const allPagesContent = declaracoesParaIncluir.map((decl, index) => {
                let content = substituirPlaceholders(decl.content, dados);
                const headerStack = [
                    { text: (dados.entidade || 'Entidade não informada').toUpperCase(), bold: true, alignment: 'center', fontSize: 14, margin: [0, 15, 0, 5] },
                    { text: `${(dados.endereco || '').toUpperCase()} - ${(dados.municipio || '').toUpperCase()}/${(dados.uf || '').toUpperCase()} - CEP: ${dados.cep || ''}`, fontSize: 10, alignment: 'center', margin: [0, 0, 0, 8] },
                    { canvas: [{ type: 'line', x1: 70, y1: 15, x2: 445, y2: 15, lineWidth: 0.5, lineColor: '#cccccc' }], margin: [0, 0, 0, 20] }
                ];

                if (['00SL_emendas', '00SL_comissao'].includes(opcaoBase) &&
                    decl.title === "DECLARAÇÃO DE TITULARIDADE DO TERRENO" &&
                    dados.espacosFisicos.length > 0) {

                    const linhasPrimeiraPagina = 5;
                    const linhasSegundaPagina = 15;
                    const gruposEspacos = [];
                    gruposEspacos.push(dados.espacosFisicos.slice(0, linhasPrimeiraPagina));
                    if (dados.espacosFisicos.length > linhasPrimeiraPagina) {
                        gruposEspacos.push(dados.espacosFisicos.slice(linhasPrimeiraPagina, linhasPrimeiraPagina + linhasSegundaPagina));
                    }
                    if (dados.espacosFisicos.length > linhasPrimeiraPagina + linhasSegundaPagina) {
                        const linhasPorPaginaRestante = 20;
                        for (let i = linhasPrimeiraPagina + linhasSegundaPagina; i < dados.espacosFisicos.length; i += linhasPorPaginaRestante) {
                            gruposEspacos.push(dados.espacosFisicos.slice(i, i + linhasPorPaginaRestante));
                        }
                    }

                    const partesTabela = gruposEspacos.map((grupo, grupoIndex) => {
                        const isPrimeiraParte = grupoIndex === 0;
                        return {
                            stack: [
                                ...(isPrimeiraParte ? [{
                                    text: content.replace(/Informo que:\n\n- Nome do Espaço Físico:.*?(?=\n|$)/s, '').trim(),
                                    alignment: 'justify', fontSize: 12, lineHeight: 1.15, margin: [0, 0, 0, 5]
                                }] : []),
                                {
                                    table: {
                                        widths: ['*', '*'],
                                        body: [
                                            [{ text: 'Nome do Espaço Físico', bold: true, fontSize: 10, fillColor: '#e6e6e6' },
                                             { text: 'Endereço do Espaço Físico', bold: true, fontSize: 10, fillColor: '#e6e6e6' }],
                                            ...grupo.map(espaco => [
                                                { text: espaco.nome || '', fontSize: 10 },
                                                { text: espaco.endereco || '', fontSize: 10 }
                                            ])
                                        ]
                                    },
                                    layout: {
                                        hLineWidth: (i, node) => (i === 0 || i === node.table.body.length) ? 1 : 0.5,
                                        vLineWidth: (i, node) => (i === 0 || i === node.table.widths.length) ? 1 : 0.5,
                                        hLineColor: (i, node) => (i === 0 || i === node.table.body.length) ? '#003087' : '#cccccc',
                                        vLineColor: (i, node) => (i === 0 || i === node.table.widths.length) ? '#003087' : '#cccccc',
                                        paddingLeft: () => 5, paddingRight: () => 5, paddingTop: () => 3, paddingBottom: () => 3
                                    },
                                    margin: [0, 65, 0, 10],
                                    pageBreak: !isPrimeiraParte ? 'before' : undefined
                                }
                            ]
                        };
                    });

                    pageNumbers.set(decl.title, currentPage);
                    currentPage += gruposEspacos.length;

                    return {
                        margin: [defaults.leftRightMargin, layoutOptions.topMargin, defaults.leftRightMargin, bottomMargin],
                        stack: [
                            ...headerStack,
                            { text: `\n${substituirPlaceholders(decl.title, dados)}`, style: 'header', alignment: 'center', margin: [0, 0, 0, 20] },
                            ...partesTabela
                        ],
                        pageBreak: index < declaracoesParaIncluir.length - 1 ? 'after' : undefined
                    };
                }

                pageNumbers.set(decl.title, currentPage);
                currentPage++;

                const formattedContent = {
                    stack: content.split('\n\n').map(p => ({
                        text: p.trim(),
                        alignment: 'justify', fontSize: 12, lineHeight: 1.15,
                        margin: [0, 0, 0, 10]
                    }))
                };

                return {
                    pageBreak: index < declaracoesParaIncluir.length - 1 ? 'after' : undefined,
                    margin: [defaults.leftRightMargin, layoutOptions.topMargin, defaults.leftRightMargin, bottomMargin],
                    stack: [
                        ...headerStack,
                        { text: `\n${substituirPlaceholders(decl.title, dados)}`, style: 'header', alignment: 'center', margin: [0, 0, 0, 20] },
                        formattedContent
                    ]
                };
            }).concat({
                pageBreak: 'before',
                margin: [defaults.leftRightMargin, 100, defaults.leftRightMargin, bottomMargin],
                stack: [
                    { text: (dados.entidade || '').toUpperCase(), bold: true, alignment: 'center', fontSize: 14, margin: [0, 0, 0, 5] },
                    { text: `${(dados.endereco || '').toUpperCase()} - ${(dados.municipio || '').toUpperCase()}/${(dados.uf || '').toUpperCase()} - CEP: ${dados.cep || ''}`, fontSize: 10, alignment: 'center', margin: [0, 0, 0, 8] },
                    { canvas: [{ type: 'line', x1: 70, y1: 15, x2: 445, y2: 15, lineWidth: 0.5, lineColor: '#cccccc' }], margin: [0, 0, 0, 20] },
                    { text: 'DECLARAÇÕES REFERENCIAIS', style: 'header', alignment: 'center', margin: [0, 30, 0, 15] },
                    { text: 'Relação das declarações contidas neste documento, assinadas eletronicamente na presente página.', style: 'subheader', alignment: 'justify', margin: [0, 5, 0, 5] },
                    {
                        table: {
                            headerRows: 1,
                            widths: [40, '*'],
                            body: [
                                [{ text: 'Página', style: 'tableHeader' }, { text: '', style: 'tableHeader' }],
                                ...declaracoesParaIncluir.map((decl, idx) => [
                                    { text: `${pageNumbers.get(decl.title)}`, alignment: 'center', fontSize: 9, fillColor: idx % 2 === 0 ? '#F5F6F5' : '#FFFFFF' },
                                    { text: ` ${substituirPlaceholders(decl.title, dados)}`, linkToPage: pageNumbers.get(decl.title), decoration: 'underline', color: '#003087', fontSize: 9, alignment: 'left', fillColor: idx % 2 === 0 ? '#F5F6F5' : '#FFFFFF' }
                                ])
                            ]
                        },
                        layout: {
                            hLineWidth: (i, node) => (i === 0 || i === node.table.body.length) ? 1.5 : 0.5,
                            vLineWidth: () => 1,
                            hLineColor: () => '#003087',
                            vLineColor: () => '#003087',
                            paddingLeft: () => 5, paddingRight: () => 5, paddingTop: () => 2, paddingBottom: () => 2
                        },
                        margin: [0, 5, 0, 5],
                        alignment: 'center'
                    },
                    { text: `Por ser verdade, firmo o teor das ${declaracoesParaIncluir.length} (${numeroParaExtenso(declaracoesParaIncluir.length)}) declarações que compõem este arquivo:`, alignment: 'justify', fontSize: 11, margin: [0, 20, 0, 20] },
                    { text: `${(dados.municipio || '').toUpperCase()}/${(dados.uf || '').toUpperCase()}, na data da assinatura eletrônica.`, alignment: 'center', fontSize: 11, margin: [0, 20, 0, 20] },
                    { text: '__________________________________________', alignment: 'center', fontSize: 11, margin: [0, 20, 0, 10] },
                    { text: dados.dirigente || '', alignment: 'center', bold: true, fontSize: 11, margin: [0, 0, 0, 10] },
                    { text: dados.cargoDirigente || '', alignment: 'center', italic: true, fontSize: 11, margin: [0, 0, 0, 10] }
                ]
            });

            const docDefinition = {
                pageSize: 'A4',
                pageMargins: [defaults.leftRightMargin, 0, defaults.leftRightMargin, bottomMargin],
                background: letterheadImage ? { image: letterheadImage, width: 595.28, height: 841.89, absolutePosition: { x: 0, y: 0 }, opacity: 1.0 } : null,
                footer: (currentPage, pageCount) => ({
                    margin: [40, 0, 40, 0],
                    stack: [
                        {
                            columns: [
                                {
                                    text: currentPage < pageCount
                                        ? 'A assinatura eletrônica será realizada exclusivamente na última página, sendo considerada válida para todas as páginas anteriores.'
                                        : `Documento composto por ${declaracoesParaIncluir.length} (${numeroParaExtenso(declaracoesParaIncluir.length)}) declarações, assinado eletronicamente nesta página, com validade jurídica para o conjunto.`,
                                    fontSize: 8.5, color: '#555555', alignment: 'left'
                                },
                                { text: `Página ${currentPage} de ${pageCount}`, fontSize: 8.5, color: '#555555', alignment: 'right' }
                            ]
                        },
                        { canvas: [{ type: 'line', x1: 0, y1: 10, x2: 515, y2: 10, lineWidth: 1, lineColor: '#003087' }] }
                    ]
                }),
                content: allPagesContent,
                styles: {
                    header: { fontSize: 14, bold: true, color: '#003087', alignment: 'center' },
                    subheader: { fontSize: 10, italic: true, color: '#333333', alignment: 'justify' },
                    tableHeader: { fontSize: 10, bold: true, color: '#FFFFFF', fillColor: '#003087', alignment: 'center' }
                },
                defaultStyle: { font: 'Roboto', alignment: 'justify' },
                permissions: { printing: 'highResolution', modifying: false, copying: false, annotating: false, fillingForms: false, contentAccessibility: false, documentAssembly: false }
            };

            const nomeArquivo = 'Declarações Consolidadas - Sem prazo de validade.pdf';
            const pdfDoc = pdfMake.createPdf(docDefinition);
            pdfDoc.download(nomeArquivo, () => {
                showToast('PDF gerado com sucesso!');
                isGeneratingPDF = false;
            }, (error) => {
                showToast('Erro ao gerar PDF: ' + error.message, true);
                isGeneratingPDF = false;
            });

        } catch (error) {
            showToast('Erro ao gerar PDF: ' + error.message, true);
            isGeneratingPDF = false;
        } finally {
            if (loadingMessage) loadingMessage.style.display = 'none';
        }
    }

    function validarDadosFormulario(dados) {
        const camposObrigatorios = ['dirigente', 'matricula', 'cargoDirigente', 'proposta', 'cnpj', 'entidade', 'endereco', 'uf', 'municipio', 'cep', 'opcaoSelecao'];
        const erros = camposObrigatorios.filter(campo => !dados[campo] || dados[campo].trim() === '').map(campo => `O campo ${campo} é obrigatório.`);
        if (!['00SL_emendas', '00SL_comissao', '20JP_emenda', '20JP_comissao', '00SL_comissao_mais65mil', '20JP_comissao_mais65mil'].includes(dados.opcaoSelecao)) {
            erros.push('A opção selecionada é inválida.');
        }
        if (dados.opcaoSelecao.startsWith('00SL') && (!dados.espacosFisicos || dados.espacosFisicos.length === 0)) {
            erros.push('Pelo menos um espaço físico deve ser informado para propostas 00SL.');
        }
        return erros;
    }

    // ═══════════════════════════════════════════════════════════
    // CONTROLE DE ABAS E STEPS
    // ═══════════════════════════════════════════════════════════
    function mostrarAba(abaId) {
        document.querySelectorAll('.tab').forEach(tab => tab.classList.remove('active'));
        document.getElementById(abaId).classList.add('active');

        const abaIndex = { 'aba1': 0, 'aba2': 1, 'aba3': 2 };
        const steps = document.querySelectorAll('.step-item');
        steps.forEach((step, idx) => {
            step.classList.remove('active', 'done');
            if (idx < abaIndex[abaId]) step.classList.add('done');
            else if (idx === abaIndex[abaId]) step.classList.add('active');
        });

        const buttons = document.querySelectorAll('.steps-nav .step-btn');
        buttons.forEach(btn => btn.setAttribute('aria-selected', 'false'));
        const currentBtn = document.querySelector(`#step${abaIndex[abaId]+1} .step-btn`);
        if (currentBtn) currentBtn.setAttribute('aria-selected', 'true');
    }

    // ═══════════════════════════════════════════════════════════
    // VALIDAÇÃO
    // ═══════════════════════════════════════════════════════════
    function validarLetras(input) {
        const erro = document.getElementById(`erro-${input.id}`);
        input.value = input.value.replace(/[^A-Za-zÀ-ÿ\s]/g, '');
        if (erro) erro.style.display = input.value ? 'none' : 'block';
    }

    function validarLetrasApenas(event) {
        const char = String.fromCharCode(event.which || event.keyCode);
        return /^[A-Za-zÀ-ÿ\s]$/.test(char) || event.preventDefault();
    }

    function validarNumeros(input) {
        const erro = document.getElementById(`erro-${input.id}`);
        input.value = input.value.replace(/\D/g, '');
        if (erro) erro.style.display = input.value ? 'none' : 'block';
    }

    function validarProposta(input) {
        const erro = document.getElementById('erro-proposta');
        let value = input.value.replace(/\D/g, '');
        if (value.length > 10) value = value.slice(0, 10);
        if (value.length > 6) value = value.slice(0, 6) + '/' + value.slice(6);
        input.value = value;
        erro.style.display = input.value && !/^\d{6}\/\d{4}$/.test(input.value) ? 'block' : 'none';
    }

    function mascaraCNPJ(input) {
        const erro = document.getElementById('erro-cnpj');
        let value = input.value.replace(/\D/g, '');
        if (value.length >= 14) {
            erro.style.display = 'none';
            fetchCNPJData(value);
        } else {
            erro.textContent = 'Digite 14 números.';
            erro.style.display = 'block';
            ['entidade', 'endereco', 'uf', 'municipio', 'cep'].forEach(id => {
                document.getElementById(id).value = '';
                document.getElementById(`erro-${id}`).style.display = 'block';
            });
        }
        let valorFormatado = '';
        if (value.length > 0) valorFormatado = value.substring(0, 2);
        if (value.length > 2) valorFormatado += '.' + value.substring(2, 5);
        if (value.length > 5) valorFormatado += '.' + value.substring(5, 8);
        if (value.length > 8) valorFormatado += '/' + value.substring(8, 12);
        if (value.length > 12) valorFormatado += '-' + value.substring(12, 14);
        input.value = valorFormatado;
    }

    async function fetchCNPJData(cnpj) {
        const erro = document.getElementById('erro-cnpj');
        try {
            const response = await fetch(`https://open.cnpja.com/office/${cnpj}`);
            if (!response.ok) throw new Error('CNPJ não encontrado');
            const data = await response.json();
            const razaoSocial = data.company?.name || '';
            const isMunicipio = razaoSocial.toLowerCase().startsWith('município de') || razaoSocial.toLowerCase().startsWith('prefeitura de');
            const entidadeVal = isMunicipio && data.address?.city && data.address?.state
                ? `Município de ${data.address.city}/${data.address.state.toUpperCase()}`
                : razaoSocial || '';
            document.getElementById('entidade').value = entidadeVal;
            document.getElementById('erro-entidade').style.display = entidadeVal ? 'none' : 'block';

            const endereco = `${data.address?.street || ''}${data.address?.number ? `, ${data.address.number}` : ''}${data.address?.district ? `, ${data.address.district}` : ''}`.trim().replace(/^,|,$/g, '');
            document.getElementById('endereco').value = endereco;
            document.getElementById('erro-endereco').style.display = endereco ? 'none' : 'block';

            const ufVal = data.address?.state?.toUpperCase() || '';
            document.getElementById('uf').value = ufVal;
            document.getElementById('erro-uf').style.display = ufVal ? 'none' : 'block';

            const municipioVal = data.address?.city || '';
            document.getElementById('municipio').value = municipioVal;
            document.getElementById('erro-municipio').style.display = municipioVal ? 'none' : 'block';

            const cepVal = data.address?.zip?.replace(/(\d{5})(\d{3})/, '$1-$2') || '';
            document.getElementById('cep').value = cepVal;
            document.getElementById('erro-cep').style.display = cepVal ? 'none' : 'block';

        } catch (error) {
            erro.textContent = 'CNPJ inválido ou não encontrado.';
            erro.style.display = 'block';
            ['entidade', 'endereco', 'uf', 'municipio', 'cep'].forEach(id => {
                document.getElementById(id).value = '';
                document.getElementById(`erro-${id}`).style.display = 'block';
            });
        }
    }

    function validarAba(abaAtual, proximaAba) {
        const aba = document.getElementById(abaAtual);
        const inputs = aba.querySelectorAll('input[required]');
        let valido = true;
        inputs.forEach(input => {
            const erro = document.getElementById(`erro-${input.id}`);
            if (!input.value.trim()) {
                if (erro) erro.style.display = 'block';
                input.classList.add('error');
                valido = false;
            } else {
                if (erro) erro.style.display = 'none';
                input.classList.remove('error');
            }
        });

        if (valido) {
            // Habilita o botão da próxima aba
            const proxStep = { 'aba2': '#step2 .step-btn', 'aba3': '#step3 .step-btn' };
            const btn = document.querySelector(proxStep[proximaAba]);
            if (btn) btn.disabled = false;
            mostrarAba(proximaAba);
        } else {
            showToast('Preencha todos os campos obrigatórios.', true);
        }
    }

    function validarGerarPDF() {
        const tipoProposta = document.querySelector('input[name="tipo_proposta"]:checked')?.value || '';
        const origem00SL = document.querySelector('input[name="origem_00SL"]:checked')?.value || '';
        const origem20JP = document.querySelector('input[name="origem_20JP"]:checked')?.value || '';
        const espacosFisicos = Array.from(document.querySelectorAll('#espacoFisicoFields .form-row')).map(row => ({
            nome: row.querySelector('input[id^="nomeEspacoFisico"]')?.value,
            endereco: row.querySelector('input[id^="enderecoEspacoFisico"]')?.value
        })).filter(e => e.nome && e.endereco);

        const gerarPDFButton = document.getElementById('gerarPDF');
        let habilitar = false;

        if (tipoProposta === '00SL' && origem00SL) {
            habilitar = espacosFisicos.length > 0;
        } else if (tipoProposta === '20JP' && origem20JP) {
            habilitar = true;
        }

        gerarPDFButton.disabled = !habilitar;

        if (habilitar) {
            gerarPDFButton.onclick = () => {
                const formData = {
                    dirigente: document.getElementById('dirigente').value,
                    matricula: document.getElementById('matricula').value,
                    cargoDirigente: document.getElementById('cargoDirigente').value,
                    proposta: document.getElementById('proposta').value,
                    cnpj: document.getElementById('cnpj').value.replace(/\D/g, ''),
                    entidade: document.getElementById('entidade').value,
                    endereco: document.getElementById('endereco').value,
                    uf: document.getElementById('uf').value,
                    municipio: document.getElementById('municipio').value,
                    cep: document.getElementById('cep').value,
                    opcaoSelecao: tipoProposta === '00SL'
                        ? `00SL_${origem00SL}${document.getElementById('municipioMais65mil00SL')?.checked ? '_mais65mil' : ''}`
                        : `20JP_${origem20JP}${document.getElementById('municipioMais65mil20JP')?.checked ? '_mais65mil' : ''}`,
                    espacosFisicos: espacosFisicos,
                    municipioMais65mil00SL: document.getElementById('municipioMais65mil00SL')?.checked || false,
                    municipioMais65mil20JP: document.getElementById('municipioMais65mil20JP')?.checked || false
                };
                gerarPDF(formData);
            };
        }
    }

    function adicionarEspacoFisico() {
        const container = document.getElementById('espacoFisicoFields');
        const index = espacoFisicoCount++;
        const newRow = document.createElement('div');
        newRow.className = 'form-row';
        newRow.id = `espacoFisicoRow${index}`;
        newRow.innerHTML = `
            <div class="half-width">
                <label class="field-label" for="nomeEspacoFisico${index}">Nome do Espaço Físico</label>
                <input type="text" id="nomeEspacoFisico${index}" class="govbr-input"
                       oninput="validarLetras(this)" required
                       aria-describedby="erro-nomeEspacoFisico${index}"
                       placeholder="Ex: Ginásio Municipal">
                <span id="erro-nomeEspacoFisico${index}" class="error-message">Campo obrigatório.</span>
            </div>
            <div class="half-width">
                <label class="field-label" for="enderecoEspacoFisico${index}">Endereço do Espaço Físico</label>
                <input type="text" id="enderecoEspacoFisico${index}" class="govbr-input" required
                       aria-describedby="erro-enderecoEspacoFisico${index}"
                       placeholder="Ex: Rua das Flores, 100">
                <span id="erro-enderecoEspacoFisico${index}" class="error-message">Campo obrigatório.</span>
            </div>
            <button type="button" class="remove-btn" onclick="removerEspacoFisico(${index})">
                <i class="fas fa-trash"></i> Remover
            </button>
        `;
        container.appendChild(newRow);
        if (container.children.length > 1) {
            document.querySelectorAll('#espacoFisicoFields .remove-btn').forEach(btn => btn.style.display = 'inline-flex');
        }
        validarGerarPDF();
    }

    function removerEspacoFisico(index) {
        const row = document.getElementById(`espacoFisicoRow${index}`);
        if (row) row.remove();
        if (document.getElementById('espacoFisicoFields').children.length <= 1) {
            document.querySelectorAll('#espacoFisicoFields .remove-btn').forEach(btn => btn.style.display = 'none');
        }
        validarGerarPDF();
    }

    function salvarEspacoFisico() {
        const espacos = document.querySelectorAll('#espacoFisicoFields .form-row');
        let valido = true;
        espacos.forEach(row => {
            const nomeInput = row.querySelector('input[id^="nomeEspacoFisico"]');
            const enderecoInput = row.querySelector('input[id^="enderecoEspacoFisico"]');
            const erroNome = document.getElementById(`erro-${nomeInput.id}`);
            const erroEndereco = document.getElementById(`erro-${enderecoInput.id}`);
            if (!nomeInput.value.trim()) { if (erroNome) erroNome.style.display = 'block'; valido = false; }
            else { if (erroNome) erroNome.style.display = 'none'; }
            if (!enderecoInput.value.trim()) { if (erroEndereco) erroEndereco.style.display = 'block'; valido = false; }
            else { if (erroEndereco) erroEndereco.style.display = 'none'; }
        });
        if (valido) { showToast('Espaços físicos salvos com sucesso!'); validarGerarPDF(); }
        else { showToast('Preencha todos os campos dos espaços físicos.', true); }
    }

    function showToast(message, isError = false) {
        const toast = document.getElementById('toast');
        toast.textContent = message;
        toast.style.backgroundColor = isError ? '#C92300' : '#168821';
        toast.className = 'toast show';
        setTimeout(() => { toast.className = 'toast'; }, 3000);
    }

    // ═══════════════════════════════════════════════════════════
    // EVENT LISTENERS — GRUPOS DE OPÇÃO
    // ═══════════════════════════════════════════════════════════
    document.querySelectorAll('.option-group-header').forEach(header => {
        header.addEventListener('click', () => {
            const input = header.querySelector('input[type="radio"]');
            if (input) input.checked = true;

            document.querySelectorAll('.sub-options').forEach(sub => sub.classList.remove('open'));
            const subOptions = document.getElementById(`sub_options_${input.value}`);
            if (subOptions) subOptions.classList.add('open');

            document.getElementById('camposEspacoFisico').style.display =
                input.value === '00SL' ? 'block' : 'none';

            atualizarOpcao();
        });
    });

    // Inicialização
    document.querySelectorAll('.sub-options').forEach(sub => sub.classList.remove('open'));
    document.getElementById('camposEspacoFisico').style.display = 'none';
    validarGerarPDF();
    </script>

</body>
</html>